import { OperationPersonChoice } from './operation-person-choice';

describe('OperationPersonChoice', () => {
  it('should create an instance', () => {
    expect(new OperationPersonChoice()).toBeTruthy();
  });
});
