import { RegionalOperatorChoice } from './regional-operator-choice';

describe('RegionalOperatorChoice', () => {
  it('should create an instance', () => {
    expect(new RegionalOperatorChoice()).toBeTruthy();
  });
});
