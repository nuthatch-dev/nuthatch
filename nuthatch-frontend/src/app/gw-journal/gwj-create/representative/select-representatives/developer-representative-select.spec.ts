import { DeveloperRepresentativeSelect } from './developer-representative-select';

describe('DeveloperRepresentativeSelect', () => {
  it('should create an instance', () => {
    expect(new DeveloperRepresentativeSelect()).toBeTruthy();
  });
});
